#!/bin/zsh

# Copy to Config/AppConfig.env for local helper scripts only.
# The shipped Android app must not read provider credentials from this file.
# User-supplied provider credentials belong in the in-app Settings screen and
# are stored only in encrypted device-local storage after install.

export NOMAD_RELIEFWEB_APP_NAME="approved-reliefweb-app-name"
